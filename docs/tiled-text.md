# Tiled Text

🔑 `it.vedph.tiled-text`

- citation (`string`)
- rows (`TextTileRow[]`):
  - y (`int`)
  - tiles (`TextTile[]`):
    - x (`int`)
    - data (`dictionary string:string`)
  - data (`dictionary string:string`)
